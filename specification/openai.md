# Спецификация взаимодействия с OpenAI API

## Назначение
Проект использует OpenAI API для двух сценариев:
1. **Генерация изображений** — полностью AI-сгенерированное изображение на основе шаблона (Image Edit API).
2. **Генерация обложек** — AI генерирует немецкий заголовок из текста статьи, затем локально накладывает текст на шаблонное фото.

Работа сосредоточена в:
- `lib/openai.js` — функции взаимодействия с API.
- `lib/imageOverlay.js` — локальный рендеринг текста на изображении.
- `routes/admin/ai-generate.js` — HTTP маршруты.

---

## Переменные окружения
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `OPENAI_API_KEY` | Ключ доступа (обязателен) | — |
| `OPENAI_IMAGE_MODEL` | Модель для генерации изображений | `gpt-image-1` |
| `OPENAI_TEXT_MODEL` | Модель для генерации текста (заголовков) | `gpt-4o-mini` |
| `OPENAI_IMAGE_SIZE` | Размер изображения | `1024x1024` |
| `OPENAI_IMAGE_QUALITY` | Качество | `high` |
| `OPENAI_IMAGE_FORMAT` | Выходной формат | `png` |

Значения читаются в `config/index.js`.

---

## Используемые эндпоинты OpenAI

### 1. Chat Completions (генерация заголовка)
**Эндпоинт:** `POST https://api.openai.com/v1/chat/completions`

**Назначение:** Генерация короткого немецкого заголовка (≤50 символов) для обложки статьи.

**Тело запроса:**
```json
{
  "model": "gpt-4o-mini",
  "messages": [
    { "role": "system", "content": "<системный промпт>" },
    { "role": "user", "content": "Kategorie: <tag>\n\nArtikelinhalt:\n<первые 1000 символов текста>" }
  ],
  "max_tokens": 60,
  "temperature": 0.7
}
```

**Системный промпт:**
- Максимум 50 символов
- Немецкий язык
- Без кавычек и точки в конце
- Профессиональный стиль

**Функция:** `generateTitle({ description, tag })` в `lib/openai.js`.

### 2. Image Edit (AI-генерация изображения)
**Эндпоинт:** `POST https://api.openai.com/v1/images/edits`

**Тело:** `FormData` с полями `image`, `prompt`, `model`, `size`, `quality`.

**Функция:** `generateImageWithTemplate({ imageBuffer, imageName, prompt })` в `lib/openai.js`.

### 3. Image Generation (без входного изображения)
**Эндпоинт:** `POST https://api.openai.com/v1/images/generations`

**Тело:** JSON `{ model, prompt, n: 1, size, quality }`.

**Функция:** `generateImage({ prompt })` в `lib/openai.js`.

---

## Локальный рендеринг текста

**Модуль:** `lib/imageOverlay.js`

**Зависимость:** `@napi-rs/canvas`

**Функция:** `renderOverlay({ imageBuffer, title, tag })`

**Стиль оверлея:**
- Полупрозрачный белый фон (`rgba(255, 255, 255, 0.85)`)
- Заголовок: тёмный текст `#0F172A`, жирный, 42px
- Тег: серый текст `#475569`, 24px
- Позиция: левый верхний угол (ниже логотипа), максимальная ширина 450px
- Тень под блоком для читаемости

---

## Маршруты

### POST `/admin/posts/:id/media/generate-ai`
**Назначение:** AI-генерация изображения (полная перегенерация через OpenAI Image Edit).

**Тело запроса:**
```json
{
  "templateId": 123,
  "title": "Заголовок статьи",
  "tag": "Finance",
  "useRandomTemplate": false
}
```

**Шаги:**
1. Проверка существования поста.
2. Выбор шаблона (заданный или случайный `is_template = 1`).
3. Сборка промпта из `prompts/default.txt` с подстановкой `{{title}}`, `{{tag}}`.
4. Вызов `generateImageWithTemplate`.
5. Сохранение в `uploads/<year>/<month>/ai_<uuid>.png`.
6. Запись в таблицу `media`.

### POST `/admin/posts/:id/media/generate-cover` ⭐ НОВЫЙ
**Назначение:** Генерация обложки — AI создаёт заголовок, локально накладывается на шаблон.

**Тело запроса:**
```json
{
  "templateId": 123,
  "tag": "Finance",
  "useRandomTemplate": false
}
```

**Шаги:**
1. Проверка существования поста и наличия текста.
2. Выбор шаблона (заданный или случайный).
3. Вызов `generateTitle` — AI генерирует немецкий заголовок (≤50 символов).
4. Вызов `renderOverlay` — локальное наложение заголовка и тега на изображение.
5. Сохранение в `uploads/<year>/<month>/cover_<uuid>.png`.
6. Запись в таблицу `media`.

**Ответ:**
```json
{
  "media_id": 456,
  "url": "http://localhost:3025/media/2025/12/cover_abc.png",
  "generated_title": "Deine Finanzen im Griff",
  "tag": "Finance"
}
```

---

## UI

На странице редактирования поста (`/admin/posts/:id`) две кнопки:
- **🤖 Create with AI** — открывает модал для полной AI-генерации изображения.
- **📷 Generate Cover** — открывает модал для генерации обложки (заголовок + оверлей).

Обе кнопки поддерживают:
- Выбор шаблона или случайный выбор.
- Выбор тега/категории.

---

## Ошибки и обработка

| Код | Ошибка | Описание |
|-----|--------|----------|
| 404 | `NOT_FOUND` | Пост не найден |
| 400 | `NO_TEMPLATES` | Нет доступных шаблонов |
| 400 | `NO_TEXT_CONTENT` | У поста нет текста (для generate-cover) |
| 500 | `TEMPLATE_FILE_MISSING` | Файл шаблона отсутствует на диске |
| 500 | `AI_GENERATION_FAILED` | Ошибка OpenAI Image API |
| 500 | `COVER_GENERATION_FAILED` | Ошибка генерации обложки |

---

## Тестирование (ручное)

### Генерация обложки (новый поток)
1. Установить `OPENAI_API_KEY`.
2. Загрузить шаблон через `/admin/foto`.
3. Создать пост с текстом статьи.
4. Открыть пост, нажать "📷 Generate Cover".
5. Выбрать тег, нажать "Generate Cover".
6. Проверить: сгенерирован немецкий заголовок, наложен на изображение, сохранён как `media`.

### Проверка ошибок
- Пост без текста → ошибка `NO_TEXT_CONTENT`.
- Удалить файл шаблона → ошибка `TEMPLATE_FILE_MISSING`.
- Очистить API-ключ → ошибка `OPENAI_API_KEY is not configured`.

---

## Соображения безопасности

- API-ключ хранится только в окружении, не логируется.
- `gpt-image-1` требует верифицированную организацию OpenAI.
- `gpt-4o-mini` доступен для всех аккаунтов.
- Рекомендуется лимитировать частоту вызовов.

---

## Архитектура

```
┌─────────────┐      ┌──────────────────┐      ┌─────────────────┐
│   UI        │─────▶│  POST            │─────▶│  generateTitle  │
│  (Button)   │      │  /generate-cover │      │  (chat/compl.)  │
└─────────────┘      └────────┬─────────┘      └────────┬────────┘
                              │                         │
                              │                         ▼
                              │               ┌─────────────────┐
                              │               │  renderOverlay  │
                              │               │  (@napi-rs/     │
                              │               │   canvas)       │
                              │               └────────┬────────┘
                              │                        │
                              ▼                        ▼
                     ┌────────────────────────────────────────┐
                     │  Save to uploads/ + INSERT INTO media  │
                     └────────────────────────────────────────┘
```
