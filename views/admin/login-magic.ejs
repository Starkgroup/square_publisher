<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Square Publisher</title>
  <link rel="stylesheet" href="/public/css/admin.css">
</head>
<body class="auth-page">
  <header class="admin-header">
    <div class="admin-brand">
      <div class="admin-brand-mark">
        <img src="/public/img/logo.png" alt="Square Capital Consulting logo" class="admin-brand-logo">
      </div>
      <div class="admin-brand-text">Square Capital Consulting</div>
    </div>
    <div class="admin-user">
      <span class="text-muted">Login</span>
    </div>
  </header>

  <main class="auth-main">
    <div class="login-container">
      <h1 class="login-title">Sign In</h1>
      
      <% if (success === 'link_sent') { %>
        <div class="success">
          If an account with this email exists, we've sent you a login link. Please check your inbox.
        </div>
      <% } %>
      
      <% if (error) { %>
        <div class="error">
          <% if (error === 'missing_email') { %>
            Please enter your email address
          <% } else { %>
            An error occurred. Please try again.
          <% } %>
        </div>
      <% } %>
      
      <p class="login-description">Enter your email and we'll send you a magic link to sign in.</p>
      
      <form method="POST" action="/login/magic">
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required autocomplete="email" placeholder="you@example.com">
        </div>
        
        <button type="submit" class="btn btn-primary">Send Login Link</button>
      </form>
      
      <div class="login-footer">
        <a href="/admin/login" class="text-muted">Admin login with password</a>
      </div>
    </div>
  </main>
  
  <style>
    .login-description {
      color: #666;
      margin-bottom: 20px;
      font-size: 14px;
    }
    .login-footer {
      margin-top: 20px;
      text-align: center;
      font-size: 13px;
    }
    .login-footer a {
      color: #666;
      text-decoration: none;
    }
    .login-footer a:hover {
      color: #2563eb;
      text-decoration: underline;
    }
    .success {
      background: #d4edda;
      border: 1px solid #c3e6cb;
      color: #155724;
      padding: 12px 16px;
      border-radius: 6px;
      margin-bottom: 20px;
      font-size: 14px;
    }
  </style>
</body>
</html>
